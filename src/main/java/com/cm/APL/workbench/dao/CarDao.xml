<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cm.APL.workbench.dao.CarDao">


    <select id="getTotal" resultType="int">
        select count(*) from car
    </select>
    <select id="getCarList" resultType="Car">
        select
            c.cid,
            c.plateNo,
            c.cplace,
            c.stage,
            d.dname as did,
            c.cload,
            c.baoxianId,
            c.cname
        from car c join driver d on c.did=d.did limit #{skipCount} , #{pageSize}

    </select>
    <insert id="save">
        insert into car(
cid,
plateNo,
cplace,
stage,
company,
did,
cload,
fdjId,
baoxianId,
description,
cname,
createBy

        )
        values(
#{cid},
#{plateNo},
#{cplace},
#{stage},
#{company},
#{did},
#{cload},
#{fdjId},
#{baoxianId},
#{description},
#{cname},
#{createBy}
   )
    </insert>




</mapper>